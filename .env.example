# Optional API token (enables auth when set)
FIRE_UAV_API_TOKEN=change-me

# Adapter base URL for Unreal bridge
FIRE_UAV_SETTINGS=fire_uav/config/settings_default.json
UNREAL_BRIDGE_PORT=9000

# Logging
FIRE_UAV_PROFILE=dev
